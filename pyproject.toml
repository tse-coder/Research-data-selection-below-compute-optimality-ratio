[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "src"
version = "1.0.0"
description = "Data selection below a compute-optimality threshold: LoRA fine-tuning with selected Alpaca subsets"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*"]
